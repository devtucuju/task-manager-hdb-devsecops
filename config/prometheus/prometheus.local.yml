# Prometheus — scrape da aplicação no host (Prometheus instalado localmente, app em :5000)
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'task-manager'
    static_configs:
      - targets: ['localhost:5000']
    metrics_path: '/metrics'

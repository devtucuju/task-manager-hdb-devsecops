# Recepção de logs remotos via UDP (porta 514)
module(load="imudp")

input(type="imudp" port="514" ruleset="remote")

ruleset(name="remote") {
    action(
        type="omfile"
        file="/var/log/remote.log"
        template="RSYSLOG_TraditionalFileFormat"
    )
}
